import { ERPModulePage } from '@/components/erp/ERPModulePage';
import type { ERPModulePageConfig } from '@/components/erp/ERPModulePage';

const config: ERPModulePageConfig = {
  title: 'Landed Cost Allocation',
  description: 'Allocate additional costs to received stock',
  businessPurpose: 'Distribute freight, customs, insurance costs to inventory items',
  financeEffect: 'stock_value',
  financeEffectDescription: 'Capitalizes costs into inventory value. Affects future COGS and margins',
  financeRegistryKey: 'landed_cost_allocation',
  module: 'inventory',
  documentType: 'Landed Cost',
  statusFlow: ['draft', 'pending_approval', 'approved', 'open', 'closed'] as any,
  columns: [
    { key: 'doc_num', label: 'Doc #', sortable: true },
    { key: 'date', label: 'Date', type: 'date', sortable: true },
    { key: 'description', label: 'Description' },
    { key: 'status', label: 'Status', type: 'status' },
  ],
  formFields: [
    { key: 'description', label: 'Description', type: 'text', required: true },
    { key: 'date', label: 'Date', type: 'date', required: true },
    { key: 'notes', label: 'Notes', type: 'textarea' },
  ],
  sampleData: [],
};

export default function Page() {
  return <ERPModulePage config={config} />;
}
